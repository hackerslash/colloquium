## 2024-05-18 - Adding ARIA labels to placeholder-only inputs
**Learning:** Found multiple inputs that relied on placeholder text without an accessible name.
**Action:** Added `aria-label` to these inputs to ensure they are properly announced by screen readers.
