## 2026-08-25 - Missing form labels
**Learning:** Found some inputs missing explicit form labels, hindering screen readers from understanding the context.
**Action:** Always ensure `<label>` elements with `htmlFor` are correctly associated with inputs and add `required` attributes and visual indicators (`*`) for mandatory fields.
